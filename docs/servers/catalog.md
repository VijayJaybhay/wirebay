# Server catalog

<!-- Generated by `npm run gen:docs` from presets/*.json. Do not edit by hand. -->

Every built-in server, grouped by category. Add one with `wirebay add <name> to <tools>`.
Servers marked *browser sign-in* open a login page the first time a tool starts them.
Some presets have their own detailed guide: [GitHub](github.md), [Netlify](netlify.md), [Firebase](firebase.md), [AWS](aws.md).
Anything not listed works too: see [custom servers](custom-servers.md).

| Category | Servers |
|---|---|
| ai | [huggingface](#huggingface) · [memory](#memory) · [sequential-thinking](#sequential-thinking) |
| browser | [chrome-devtools](#chrome-devtools) · [playwright](#playwright) |
| cloud | [aws-api](#aws-api) · [aws-docs](#aws-docs) · [azure](#azure) · [cloudflare](#cloudflare) · [cloudflare-docs](#cloudflare-docs) · [firebase](#firebase) · [heroku](#heroku) · [kubernetes](#kubernetes) · [netlify](#netlify) · [render](#render) · [terraform](#terraform) · [vercel](#vercel) |
| code-hosting | [github](#github) · [gitlab](#gitlab) |
| databases | [mongodb](#mongodb) · [neon](#neon) · [postgres](#postgres) · [supabase](#supabase) · [upstash](#upstash) |
| dev-tools | [context7](#context7) · [docker](#docker) · [git](#git) · [postman](#postman) · [shopify-dev](#shopify-dev) |
| observability | [sentry](#sentry) |
| payments | [paypal](#paypal) · [stripe](#stripe) |
| productivity | [atlassian](#atlassian) · [linear](#linear) · [notion](#notion) |
| search | [brave-search](#brave-search) · [exa](#exa) · [firecrawl](#firecrawl) · [perplexity](#perplexity) · [tavily](#tavily) |
| utilities | [fetch](#fetch) · [filesystem](#filesystem) · [time](#time) |

## huggingface

Hugging Face: search models, datasets, papers and Spaces

**Category:** ai · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://huggingface.co/docs/hub/hf-mcp-server)

```bash
wirebay add huggingface to all
```

- **Runs:** hosted server `https://huggingface.co/mcp` (no auth), bridged with mcp-remote
- **`--variant token`:** hosted server `https://huggingface.co/mcp` (`HF_TOKEN`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `HF_TOKEN` | no | Access token (token variant) | [link](https://huggingface.co/settings/tokens) |

Set with `wirebay secrets set <KEY>`.

> Anonymous by default; `--variant token` uses HF_TOKEN. Choose tools at huggingface.co/settings/mcp.

## memory

Memory: persistent knowledge-graph memory across sessions (MCP reference server)

**Category:** ai · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/modelcontextprotocol/servers/tree/main/src/memory)

```bash
wirebay add memory to all
```

- **Runs:** `npx -y @modelcontextprotocol/server-memory@2026.8.31`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `MEMORY_FILE_PATH` | no | Optional: where to store the memory file |  |

Set with `wirebay secrets set <KEY>`.

> Stores its knowledge graph in a local file (MEMORY_FILE_PATH).

## sequential-thinking

Sequential Thinking: structured step-by-step reasoning tool (MCP reference server)

**Category:** ai · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://github.com/modelcontextprotocol/servers/tree/main/src/sequentialthinking)

```bash
wirebay add sequential-thinking to all
```

- **Runs:** `npx -y @modelcontextprotocol/server-sequential-thinking@2026.8.31`
- **Needs:** npx

## chrome-devtools

Chrome DevTools: inspect and debug live Chrome (performance, network, console)

**Category:** browser · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://github.com/ChromeDevTools/chrome-devtools-mcp)

```bash
wirebay add chrome-devtools to all
```

- **Runs:** `npx -y chrome-devtools-mcp@1.10.1 --no-usage-statistics`
- **Needs:** npx

> Needs Chrome installed. wirebay starts it with `--no-usage-statistics`. Performance tools send trace URLs to Google CrUX.

## playwright

Playwright: automate a real browser via accessibility snapshots (Microsoft)

**Category:** browser · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/microsoft/playwright-mcp)

```bash
wirebay add playwright to all
```

- **Runs:** `npx -y @playwright/mcp@0.0.82 [--browser ${PLAYWRIGHT_BROWSER}]`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `PLAYWRIGHT_BROWSER` | no | Optional: chrome, firefox, webkit or msedge |  |

Set with `wirebay secrets set <KEY>`.

> Drives a real browser and can submit forms in logged-in sessions. Set PLAYWRIGHT_BROWSER to pick a browser.

## aws-api

AWS API: run AWS CLI commands through MCP (read-only by default)

**Category:** cloud · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://github.com/awslabs/mcp/tree/main/src/aws-api-mcp-server)

```bash
wirebay add aws-api to all
```

- **Runs:** `uvx awslabs.aws-api-mcp-server@1.5.5`
- **Needs:** uvx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `AWS_PROFILE` | no | Named profile from ~/.aws (preferred over static keys) |  |
| `AWS_REGION` | yes | Default region, e.g. us-east-1 |  |
| `READ_OPERATIONS_ONLY` | no | true = only read/list/describe operations |  |
| `AWS_ACCESS_KEY_ID` | no | Static key (fallback only) |  |
| `AWS_SECRET_ACCESS_KEY` | no | Static secret (fallback only) |  |
| `AWS_SESSION_TOKEN` | no | Session token for temporary credentials |  |

Set with `wirebay secrets set <KEY>`.

## aws-docs

AWS Documentation: search and read official AWS docs (no credentials needed)

**Category:** cloud · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://github.com/awslabs/mcp/tree/main/src/aws-documentation-mcp-server)

```bash
wirebay add aws-docs to all
```

- **Runs:** `uvx awslabs.aws-documentation-mcp-server@1.2.1`
- **Needs:** uvx

## azure

Azure: work with 45+ Azure services using your Azure login (read-only by default)

**Category:** cloud · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/microsoft/mcp/tree/main/servers/Azure.Mcp.Server)

```bash
wirebay add azure to all
```

- **Runs:** `npx -y @azure/mcp@2.0.5 server start --read-only`
- **`--variant read-write`:** `npx -y @azure/mcp@2.0.5 server start`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `AZURE_TENANT_ID` | no | Service principal tenant (optional; default is your `az login`) |  |
| `AZURE_CLIENT_ID` | no | Service principal client ID (optional) |  |
| `AZURE_CLIENT_SECRET` | no | Service principal secret (optional) |  |

Set with `wirebay secrets set <KEY>`.

> Read-only by default; `--variant read-write` allows changes. Signs in with `az login` or a service principal. Telemetry is turned off.

## cloudflare

Cloudflare API: the whole Cloudflare API via search/execute tools

**Category:** cloud · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://developers.cloudflare.com/agents/model-context-protocol/mcp-servers-for-cloudflare/)

```bash
wirebay add cloudflare to all
```

- **Runs:** hosted server `https://mcp.cloudflare.com/mcp` (browser sign-in), bridged with mcp-remote
- **`--variant bindings`:** hosted server `https://bindings.mcp.cloudflare.com/mcp` (browser sign-in), bridged with mcp-remote
- **`--variant observability`:** hosted server `https://observability.mcp.cloudflare.com/mcp` (browser sign-in), bridged with mcp-remote
- **Needs:** npx

> Full Cloudflare API (including deletes) through browser sign-in. Variants: `bindings` (Workers, KV, R2, D1) and `observability` (logs and analytics).

## cloudflare-docs

Cloudflare Docs: search Cloudflare documentation (no login needed)

**Category:** cloud · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://developers.cloudflare.com/agents/model-context-protocol/mcp-servers-for-cloudflare/)

```bash
wirebay add cloudflare-docs to all
```

- **Runs:** hosted server `https://docs.mcp.cloudflare.com/mcp` (no auth), bridged with mcp-remote
- **Needs:** npx

> Public and read-only; no login needed.

## firebase

Firebase: projects, Auth, Firestore, Data Connect, Storage, Remote Config, Crashlytics

**Category:** cloud · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://firebase.google.com/docs/ai-assistance/mcp-server)

```bash
wirebay add firebase to all
```

- **Runs:** `npx -y firebase-tools@15.31.0 mcp [--dir ${FIREBASE_PROJECT_DIR}]`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `GOOGLE_APPLICATION_CREDENTIALS` | no | Path to a service-account JSON (leave empty to use `firebase login`) | [link](https://github.com/VijayJaybhay/wirebay/blob/main/docs/servers/firebase.md#1-choose-how-to-authenticate) |
| `FIREBASE_PROJECT_DIR` | no | Folder containing firebase.json |  |

Set with `wirebay secrets set <KEY>`.

## heroku

Heroku: apps, dynos, add-ons and Postgres

**Category:** cloud · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://devcenter.heroku.com/articles/heroku-remote-mcp-server)

```bash
wirebay add heroku to all
```

- **Runs:** hosted server `https://mcp.heroku.com/mcp` (browser sign-in), bridged with mcp-remote
- **`--variant local`:** `npx -y @heroku/mcp-server@1.2.9`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `HEROKU_API_KEY` | no | API key (local variant only): `heroku authorizations:create` |  |

Set with `wirebay secrets set <KEY>`.

> Can scale, deploy and delete apps. `--variant local` uses HEROKU_API_KEY instead of browser sign-in.

## kubernetes

Kubernetes: pods, resources and Helm via your kubeconfig (containers/kubernetes-mcp-server)

**Category:** cloud · **Status:** beta · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/containers/kubernetes-mcp-server)

```bash
wirebay add kubernetes to all
```

- **Runs:** `npx -y kubernetes-mcp-server@0.0.67 [--config ${K8S_MCP_CONFIG}]`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `KUBECONFIG` | no | Optional: kubeconfig path (default ~/.kube/config) |  |
| `K8S_MCP_CONFIG` | no | Optional: TOML config, e.g. with read_only = true |  |

Set with `wirebay secrets set <KEY>`.

> Uses your kubeconfig and can change or delete resources. For read-only, create a TOML file with `read_only = true` and set K8S_MCP_CONFIG to its path.

## netlify

Netlify: sites, deploys, environment variables, forms, extensions

**Category:** cloud · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://docs.netlify.com/build/build-with-ai/netlify-mcp-server/)

```bash
wirebay add netlify to all
```

- **Runs:** `npx -y @netlify/mcp@1.15.1`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `NETLIFY_PERSONAL_ACCESS_TOKEN` | yes | Netlify personal access token | [link](https://github.com/VijayJaybhay/wirebay/blob/main/docs/servers/netlify.md#1-create-a-token) |

Set with `wirebay secrets set <KEY>`.

## render

Render: services, deploys, logs, metrics and Postgres queries

**Category:** cloud · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://render.com/docs/mcp-server)

```bash
wirebay add render to all
```

- **Runs:** hosted server `https://mcp.render.com/mcp` (`RENDER_API_KEY`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `RENDER_API_KEY` | yes | Render API key | [link](https://dashboard.render.com/u/settings#api-keys) |

Set with `wirebay secrets set <KEY>`.

> Can create services, trigger deploys and edit environment variables. SQL queries are read-only. Choose the workspace in your first prompt.

## terraform

Terraform: Registry lookups and HCP Terraform workspaces (HashiCorp); runs disabled by default

**Category:** cloud · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/hashicorp/terraform-mcp-server)

```bash
wirebay add terraform to all
```

- **Runs:** `docker run -i --rm -e TFE_TOKEN -e TFE_ADDRESS -e ENABLE_TF_OPERATIONS hashicorp/terraform-mcp-server:1.3.0`
- **Needs:** docker

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `TFE_TOKEN` | no | HCP Terraform / Enterprise API token (optional; registry tools work without) |  |
| `TFE_ADDRESS` | no | Terraform Enterprise URL (empty = HCP Terraform) |  |
| `ENABLE_TF_OPERATIONS` | no | true to allow runs/applies (default false) |  |

Set with `wirebay secrets set <KEY>`.

> Runs Docker. Registry tools work without a token; set TFE_TOKEN for HCP Terraform. Applies/destroys stay off unless ENABLE_TF_OPERATIONS=true.

## vercel

Vercel: docs, projects, deployments and logs

**Category:** cloud · **Status:** beta · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://vercel.com/docs/agent-resources/vercel-mcp)

```bash
wirebay add vercel to all
```

- **Runs:** hosted server `https://mcp.vercel.com` (browser sign-in), bridged with mcp-remote
- **Needs:** npx

> Beta; Vercel only allows approved clients. Can deploy code and make purchases.

## github

GitHub: repositories, issues, pull requests, Actions, code security

**Category:** code-hosting · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://github.com/github/github-mcp-server)

```bash
wirebay add github to all
```

- **Runs:** hosted server `${GITHUB_MCP_URL:-https://api.githubcopilot.com/mcp/}` (`GITHUB_PERSONAL_ACCESS_TOKEN`), bridged with mcp-remote
- **`--variant docker`:** `docker run -i --rm -e GITHUB_PERSONAL_ACCESS_TOKEN -e GITHUB_TOOLSETS -e GITHUB_HOST ghcr.io/github/github-mcp-server:v1.12.2`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `GITHUB_PERSONAL_ACCESS_TOKEN` | yes | Fine-grained personal access token | [link](https://github.com/VijayJaybhay/wirebay/blob/main/docs/servers/github.md#1-create-a-token) |
| `GITHUB_TOOLSETS` | no | Comma-separated toolsets to enable |  |
| `GITHUB_MCP_URL` | no | GitHub Enterprise MCP endpoint (empty = github.com) |  |
| `GITHUB_HOST` | no | GitHub Enterprise Server hostname (docker variant only) |  |

Set with `wirebay secrets set <KEY>`.

## gitlab

GitLab: issues, merge requests, CI pipelines and repositories

**Category:** code-hosting · **Status:** beta · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://docs.gitlab.com/user/model_context_protocol/mcp_server/)

```bash
wirebay add gitlab to all
```

- **Runs:** hosted server `${GITLAB_MCP_URL:-https://gitlab.com/api/v4/mcp}` (browser sign-in), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `GITLAB_MCP_URL` | no | Self-managed GitLab: https://<host>/api/v4/mcp (empty = gitlab.com) |  |

Set with `wirebay secrets set <KEY>`.

> Beta (GitLab 18.6+). Signs in through the browser. For self-managed GitLab set GITLAB_MCP_URL.

## mongodb

MongoDB: query databases and manage Atlas (read-only by default)

**Category:** databases · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/mongodb-js/mongodb-mcp-server)

```bash
wirebay add mongodb to all
```

- **Runs:** `npx -y mongodb-mcp-server@3.0.4`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `MDB_MCP_CONNECTION_STRING` | no | mongodb:// or mongodb+srv:// connection string (or use the Atlas keys) |  |
| `MDB_MCP_API_CLIENT_ID` | no | Atlas service account client ID (optional) |  |
| `MDB_MCP_API_CLIENT_SECRET` | no | Atlas service account client secret (optional) |  |
| `MDB_MCP_READ_ONLY` | no | true (default) or false |  |

Set with `wirebay secrets set <KEY>`.

> Read-only by default (MDB_MCP_READ_ONLY=true). Set either MDB_MCP_CONNECTION_STRING or the Atlas service-account pair.

## neon

Neon: serverless Postgres projects, branches, SQL and migrations (read-only by default)

**Category:** databases · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://neon.com/docs/ai/neon-mcp-server)

```bash
wirebay add neon to all
```

- **Runs:** hosted server `https://mcp.neon.tech/mcp?readonly=true` (browser sign-in), bridged with mcp-remote
- **`--variant token`:** hosted server `https://mcp.neon.tech/mcp?readonly=true` (`NEON_API_KEY`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `NEON_API_KEY` | no | Neon API key (token variant only) | [link](https://console.neon.tech/app/settings?modal=create_api_key) |

Set with `wirebay secrets set <KEY>`.

> Read-only by default. Signs in through the browser; `--variant token` uses NEON_API_KEY. Intended for development, not production.

## postgres

PostgreSQL (Postgres MCP Pro): query, explain and tune; restricted (read-only) by default

**Category:** databases · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://github.com/crystaldba/postgres-mcp)

```bash
wirebay add postgres to all
```

- **Runs:** `uvx postgres-mcp@0.3.0 --access-mode=${POSTGRES_ACCESS_MODE:-restricted}`
- **`--variant docker`:** `docker run -i --rm -e DATABASE_URI crystaldba/postgres-mcp:0.3.0 --access-mode=${POSTGRES_ACCESS_MODE:-restricted}`
- **Needs:** uvx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `DATABASE_URI` | yes | postgresql://user:password@host:5432/db |  |
| `POSTGRES_ACCESS_MODE` | no | restricted (default, read-only) or unrestricted |  |

Set with `wirebay secrets set <KEY>`.

> Restricted (read-only) by default. Set POSTGRES_ACCESS_MODE=unrestricted to allow writes. Requires uv (or `--variant docker`).

## supabase

Supabase: SQL, migrations, branches, edge functions and docs (read-only by default)

**Category:** databases · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://supabase.com/docs/guides/getting-started/mcp)

```bash
wirebay add supabase to all
```

- **Runs:** hosted server `https://mcp.supabase.com/mcp?read_only=true` (browser sign-in), bridged with mcp-remote
- **`--variant token`:** hosted server `https://mcp.supabase.com/mcp?read_only=true` (`SUPABASE_ACCESS_TOKEN`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `SUPABASE_ACCESS_TOKEN` | no | Personal access token (token variant only) | [link](https://supabase.com/dashboard/account/tokens) |

Set with `wirebay secrets set <KEY>`.

> Read-only by default (`read_only=true`). Signs in through the browser; `--variant token` uses SUPABASE_ACCESS_TOKEN. Supabase advises against connecting to production.

## upstash

Upstash: manage Redis, QStash and Workflow resources

**Category:** databases · **Status:** stable · **Auth:** 2 keys · **Verified:** 2026-09-25 · [Official docs](https://github.com/upstash/mcp-server)

```bash
wirebay add upstash to all
```

- **Runs:** `npx -y @upstash/mcp-server@0.3.0`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `UPSTASH_EMAIL` | yes | Upstash account email |  |
| `UPSTASH_API_KEY` | yes | Management API key (a read-only key disables write tools) | [link](https://console.upstash.com/account/api) |

Set with `wirebay secrets set <KEY>`.

> Can create and delete databases. A read-only management key disables all write tools.

## context7

Context7: up-to-date library documentation and code examples for LLMs

**Category:** dev-tools · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/upstash/context7)

```bash
wirebay add context7 to all
```

- **Runs:** `npx -y @upstash/context7-mcp@4.1.1`
- **`--variant remote`:** hosted server `https://mcp.context7.com/mcp` (`CONTEXT7_API_KEY`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `CONTEXT7_API_KEY` | no | Optional API key for higher rate limits (remote variant) | [link](https://context7.com/dashboard) |

Set with `wirebay secrets set <KEY>`.

> Read-only. Queries are sent to Upstash. A free API key raises rate limits: use `wirebay add context7 --variant remote` after `wirebay secrets set CONTEXT7_API_KEY`.

## docker

Docker MCP Toolkit gateway: one entry point to containerized catalog servers

**Category:** dev-tools · **Status:** beta · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://docs.docker.com/ai/mcp-catalog-and-toolkit/)

```bash
wirebay add docker to all
```

- **Runs:** `docker mcp gateway run [--profile ${DOCKER_MCP_PROFILE}]`
- **Needs:** docker

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `DOCKER_MCP_PROFILE` | no | Optional: MCP Toolkit profile to serve |  |

Set with `wirebay secrets set <KEY>`.

> Requires Docker Desktop 4.62+ with the MCP Toolkit. Secrets for catalog servers are managed by Docker, not wirebay.

## git

Git: read and operate on a local git repository (MCP reference server)

**Category:** dev-tools · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/modelcontextprotocol/servers/tree/main/src/git)

```bash
wirebay add git to all
```

- **Runs:** `uvx mcp-server-git@2026.8.18 [--repository ${GIT_REPOSITORY}]`
- **Needs:** uvx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `GIT_REPOSITORY` | no | Optional: default repository path |  |

Set with `wirebay secrets set <KEY>`.

> Can commit, reset and check out branches. Requires uv.

## postman

Postman: collections, workspaces and API specs (minimal toolset by default)

**Category:** dev-tools · **Status:** beta · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://learning.postman.com/docs/developer/postman-api/postman-mcp-server/)

```bash
wirebay add postman to all
```

- **Runs:** hosted server `https://mcp.postman.com/minimal` (browser sign-in), bridged with mcp-remote
- **`--variant full`:** hosted server `https://mcp.postman.com/mcp` (browser sign-in), bridged with mcp-remote
- **`--variant local`:** `npx -y @postman/postman-mcp-server@2.13.0`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `POSTMAN_API_KEY` | no | API key (local variant only) |  |

Set with `wirebay secrets set <KEY>`.

> Minimal toolset by default; `--variant full` can edit and delete collections. EU accounts need the local variant with POSTMAN_API_KEY and `--region eu`.

## shopify-dev

Shopify Dev: Shopify docs, GraphQL schemas and code validation (no store access)

**Category:** dev-tools · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://shopify.dev/docs/apps/build/devmcp)

```bash
wirebay add shopify-dev to all
```

- **Runs:** `npx -y @shopify/dev-mcp@1.15.4`
- **Needs:** npx

> Docs and schema tools only; no access to store data. No credentials needed.

## sentry

Sentry: errors, issues, traces and Seer root-cause analysis

**Category:** observability · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://mcp.sentry.dev)

```bash
wirebay add sentry to all
```

- **Runs:** hosted server `https://mcp.sentry.dev/mcp` (browser sign-in), bridged with mcp-remote
- **`--variant local`:** `npx -y @sentry/mcp-server@0.41.0`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `SENTRY_ACCESS_TOKEN` | no | User auth token (local variant only) | [link](https://sentry.io/settings/account/api/auth-tokens/) |
| `SENTRY_HOST` | no | Self-hosted Sentry host (local variant, optional) |  |

Set with `wirebay secrets set <KEY>`.

> Signs in through the browser. For self-hosted Sentry use `--variant local` with SENTRY_ACCESS_TOKEN and SENTRY_HOST.

## paypal

PayPal: invoices, orders and payments (sandbox by default)

**Category:** payments · **Status:** beta · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://developer.paypal.com/ai-tools/mcp-server)

```bash
wirebay add paypal to all
```

- **Runs:** hosted server `https://mcp.sandbox.paypal.com/mcp` (browser sign-in), bridged with mcp-remote
- **`--variant production`:** hosted server `https://mcp.paypal.com/mcp` (browser sign-in), bridged with mcp-remote
- **Needs:** npx

> Uses the PayPal **sandbox** by default. `--variant production` targets live payments (high risk).

## stripe

Stripe: payments API and docs search (prefer a sandbox)

**Category:** payments · **Status:** beta · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://docs.stripe.com/mcp)

```bash
wirebay add stripe to all
```

- **Runs:** hosted server `https://mcp.stripe.com` (browser sign-in), bridged with mcp-remote
- **`--variant token`:** hosted server `https://mcp.stripe.com` (`STRIPE_AGENT_API_KEY`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `STRIPE_AGENT_API_KEY` | no | Agent API key (token variant only; plain sk_ keys are rejected from 2026-10-31) | [link](https://docs.stripe.com/mcp) |

Set with `wirebay secrets set <KEY>`.

> High risk: can create refunds, subscriptions and payment links. Prefer a sandbox. From 2026-10-31 only Agent API keys or browser sign-in work.

## atlassian

Atlassian Rovo: Jira, Confluence, Jira Service Management, Bitbucket

**Category:** productivity · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://support.atlassian.com/atlassian-rovo-mcp-server/docs/getting-started-with-the-atlassian-remote-mcp-server/)

```bash
wirebay add atlassian to all
```

- **Runs:** hosted server `https://mcp.atlassian.com/v2/mcp` (browser sign-in), bridged with mcp-remote
- **Needs:** npx

> Signs in through the browser and acts with your full Jira/Confluence permissions.

## linear

Linear: issues, projects, cycles and comments

**Category:** productivity · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://linear.app/docs/mcp)

```bash
wirebay add linear to all
```

- **Runs:** hosted server `https://mcp.linear.app/mcp` (browser sign-in), bridged with mcp-remote
- **`--variant readonly`:** hosted server `https://mcp.linear.app/mcp/readonly` (browser sign-in), bridged with mcp-remote
- **`--variant token`:** hosted server `https://mcp.linear.app/mcp` (`LINEAR_API_KEY`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `LINEAR_API_KEY` | no | Personal API key (token variant only) | [link](https://linear.app/settings/account/security) |

Set with `wirebay secrets set <KEY>`.

> Signs in through the browser. `--variant readonly` exposes only read tools; `--variant token` uses LINEAR_API_KEY.

## notion

Notion: search, read and edit pages and databases

**Category:** productivity · **Status:** stable · **Auth:** browser login · **Verified:** 2026-09-25 · [Official docs](https://developers.notion.com/docs/get-started-with-mcp)

```bash
wirebay add notion to all
```

- **Runs:** hosted server `https://mcp.notion.com/mcp` (browser sign-in), bridged with mcp-remote
- **Needs:** npx

> Signs in through the browser and can create and edit pages. Workspace owners control access in Settings > Connections.

## brave-search

Brave Search: web, news, image and local search

**Category:** search · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://github.com/brave/brave-search-mcp-server)

```bash
wirebay add brave-search to all
```

- **Runs:** `npx -y @brave/brave-search-mcp-server@2.1.4 --transport stdio`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `BRAVE_API_KEY` | yes | Brave Search API key | [link](https://api-dashboard.search.brave.com/app/keys) |

Set with `wirebay secrets set <KEY>`.

> Read-only.

## exa

Exa: AI web search and page fetching

**Category:** search · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://exa.ai/docs/reference/exa-mcp)

```bash
wirebay add exa to all
```

- **Runs:** hosted server `https://mcp.exa.ai/mcp` (no auth), bridged with mcp-remote
- **`--variant key`:** hosted server `https://mcp.exa.ai/mcp` (`EXA_API_KEY`), bridged with mcp-remote
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `EXA_API_KEY` | no | API key for higher limits (key variant) | [link](https://dashboard.exa.ai/api-keys) |

Set with `wirebay secrets set <KEY>`.

> Works without a key (rate-limited). For higher limits: `wirebay secrets set EXA_API_KEY` and `--variant key`.

## firecrawl

Firecrawl: scrape, crawl, search and extract web data

**Category:** search · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://docs.firecrawl.dev/mcp-server)

```bash
wirebay add firecrawl to all
```

- **Runs:** hosted server `https://mcp.firecrawl.dev/v2/mcp` (`FIRECRAWL_API_KEY`), bridged with mcp-remote
- **`--variant local`:** `npx -y firecrawl-mcp@3.25.5`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `FIRECRAWL_API_KEY` | yes | Firecrawl API key | [link](https://www.firecrawl.dev/app/api-keys) |

Set with `wirebay secrets set <KEY>`.

> Read-only; uses credits. `--variant local` runs the npm package instead of the hosted server.

## perplexity

Perplexity: search, ask, research and reasoning

**Category:** search · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://docs.perplexity.ai/guides/mcp-server)

```bash
wirebay add perplexity to all
```

- **Runs:** hosted server `https://api.perplexity.ai/mcp` (`PERPLEXITY_API_KEY`), bridged with mcp-remote
- **`--variant local`:** `npx -y @perplexity-ai/mcp-server@1.2.1`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `PERPLEXITY_API_KEY` | yes | Perplexity API key | [link](https://console.perplexity.ai/project/keys) |

Set with `wirebay secrets set <KEY>`.

> Read-only; billed per use.

## tavily

Tavily: search, extract and crawl for agents

**Category:** search · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://docs.tavily.com/documentation/mcp)

```bash
wirebay add tavily to all
```

- **Runs:** hosted server `https://mcp.tavily.com/mcp/` (`TAVILY_API_KEY`), bridged with mcp-remote
- **`--variant local`:** `npx -y tavily-mcp@0.2.22`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `TAVILY_API_KEY` | yes | Tavily API key | [link](https://app.tavily.com) |

Set with `wirebay secrets set <KEY>`.

> Read-only; uses credits.

## fetch

Fetch: download web pages and convert them to markdown (MCP reference server)

**Category:** utilities · **Status:** stable · **Auth:** none · **Verified:** 2026-09-25 · [Official docs](https://github.com/modelcontextprotocol/servers/tree/main/src/fetch)

```bash
wirebay add fetch to all
```

- **Runs:** `uvx mcp-server-fetch@2026.8.18`
- **Needs:** uvx

> Can reach local and internal network addresses (SSRF risk). Requires uv.

## filesystem

Filesystem: read, write and search files inside an allowed folder (MCP reference server)

**Category:** utilities · **Status:** stable · **Auth:** token · **Verified:** 2026-09-25 · [Official docs](https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem)

```bash
wirebay add filesystem to all
```

- **Runs:** `npx -y @modelcontextprotocol/server-filesystem@2026.8.31 ${FILESYSTEM_ROOT}`
- **Needs:** npx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `FILESYSTEM_ROOT` | yes | Folder the server may access (not a secret) |  |

Set with `wirebay secrets set <KEY>`.

> Can create, edit and move files inside FILESYSTEM_ROOT. Point it at a project folder, never your whole home folder.

## time

Time: current time and timezone conversion (MCP reference server)

**Category:** utilities · **Status:** stable · **Auth:** optional · **Verified:** 2026-09-25 · [Official docs](https://github.com/modelcontextprotocol/servers/tree/main/src/time)

```bash
wirebay add time to all
```

- **Runs:** `uvx mcp-server-time@2026.8.18 [--local-timezone=${LOCAL_TIMEZONE}]`
- **Needs:** uvx

| Key | Required | What it is | Where to get it |
|---|---|---|---|
| `LOCAL_TIMEZONE` | no | Optional IANA timezone, e.g. Europe/Berlin |  |

Set with `wirebay secrets set <KEY>`.

