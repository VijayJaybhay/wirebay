# Warp

The Warp terminal's agents. Official docs: https://docs.warp.dev/knowledge-and-collaboration/mcp

> **Status:** beta: Windows/Linux paths aren't stated separately in the docs. If something is off, please [report it](https://github.com/pragnalabs-ai/wirebay/issues/new?template=config-changed.yml).

## Where the config lives

| Scope   | File                        |
| ------- | --------------------------- |
| user    | `~/.warp/.mcp.json`         |
| project | `<project>/.warp/.mcp.json` |

Warp can also read other tools' files (Claude Code, Codex). If you already sync to those, you may
see servers twice; turn off "Auto-spawn servers from third-party agents" or sync to only one.

## How wirebay syncs it

```bash
wirebay add github to warp
```

## Doing it by hand

```json
{
  "mcpServers": {
    "github": { "command": "wirebay", "args": ["run", "github"] }
  }
}
```

On Windows, entries written by hand usually need `cmd /c wirebay run <server>`; wirebay's own entries call `node.exe` directly.

## Verify

Settings → Agents → MCP servers shows each server with Start/Stop controls.

## Quirks

- Servers added in Warp's UI may be stored elsewhere (Warp Drive); wirebay only manages the file.

## Shared config files

<!-- generated:reads -->
<!-- This section is generated by `npm run gen:docs` from `alsoReads` in the tool manifests. -->

- Reads **Claude Code's global** config (when "Auto-spawn servers from third-party agents" is on): starts Claude Code's servers when auto-spawn for third-party agents is on. ([source](https://docs.warp.dev/knowledge-and-collaboration/mcp))
- Reads **OpenAI Codex (CLI, IDE extension and desktop app)'s global** config (when "Auto-spawn servers from third-party agents" is on): starts Codex's servers when auto-spawn for third-party agents is on. ([source](https://docs.warp.dev/knowledge-and-collaboration/mcp))
- Reads **Claude Code's project** config (after approval): offers the project .mcp.json servers for approval. ([source](https://docs.warp.dev/knowledge-and-collaboration/mcp))

wirebay still writes each tool's own file, so a server can be listed twice in a tool that reads several files.
<!-- /generated:reads -->

## Changelog

- 2026-09-25: first version, from the official docs linked above.
