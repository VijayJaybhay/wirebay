{
  "context_servers": {
    "github": {
      "command": "{{NODE}}",
      "args": [
        "{{WIREBAY_CLI}}",
        "run",
        "github"
      ]
    },
    "netlify": {
      "command": "{{NODE}}",
      "args": [
        "{{WIREBAY_CLI}}",
        "run",
        "netlify"
      ]
    }
  }
}
