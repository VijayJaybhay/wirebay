# Continue

The Continue extension for VS Code and JetBrains. Official docs: https://docs.continue.dev/customize/deep-dives/mcp

> **Status:** beta. If something is off, please [report it](https://github.com/pragnalabs-ai/wirebay/issues/new?template=config-changed.yml).

## Where the config lives

| Scope   | File                                            |
| ------- | ----------------------------------------------- |
| user    | `~/.continue/mcpServers/wirebay.json`           |
| project | `<workspace>/.continue/mcpServers/wirebay.json` |

Continue's main `~/.continue/config.yaml` stores servers as a **list**, which wirebay doesn't edit.
But Continue reads any JSON file in `.continue/mcpServers/` written in the Claude Desktop/Cursor
format, so wirebay manages its own `wirebay.json` there.

## How wirebay syncs it

```bash
wirebay add github to continue             # global
wirebay add github to continue --project   # this workspace
```

## Doing it by hand

```json
{
  "mcpServers": {
    "github": { "command": "wirebay", "args": ["run", "github"] }
  }
}
```

On Windows, entries written by hand usually need `cmd /c wirebay run <server>`; wirebay's own entries call `node.exe` directly.

## Verify

MCP tools only work in **agent mode**. Ask the agent to use the server and check that the tools appear.

## Quirks

- The Continue **IDE extensions** scan `mcpServers/` folders; the `cn` CLI does not (continuedev/continue#12254), so use `config.yaml` for the CLI.

## Shared config files

<!-- generated:reads -->
<!-- This section is generated by `npm run gen:docs` from `alsoReads` in the tool manifests. -->

No other tool is known to read this tool's MCP files, and it reads only its own.
<!-- /generated:reads -->

## Changelog

- 2026-09-26: added the global `~/.continue/mcpServers/` folder (scanned by the IDE extensions).
- 2026-09-25: first version, from the official docs linked above.
